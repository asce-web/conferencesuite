langcode: en
status: true
dependencies:
  config:
    - field.storage.node.field_session_url
    - node.type.session
  module:
    - link
id: node.session.field_session_url
field_name: field_session_url
entity_type: node
bundle: session
label: 'Custom URL'
description: 'Provide a custom URL for the link. If left blank, this session’s node page will be used.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings:
  link_type: 1
  title: 0
field_type: link
